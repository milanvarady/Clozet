/** Number of underscores for each answer blank across all export formats. */
export const ANSWER_UNDERSCORE_COUNT = 40;

/** Separator between words in the word bank (plain text fallback only). */
export const WORD_BANK_SEPARATOR = ' / ';

/** Number of columns in the word bank grid (visual formats). */
export const WORD_BANK_COLUMNS = 4;
